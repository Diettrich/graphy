import Graph from "./graph";

const testAndDemo = (graphInstance: Graph): void => {
    let x = 10.5;
    // graphInstance.setHorizontalOffset(x);
    // graphInstance.drawDashLine();
    // graphInstance.drawAxis();
    // setInterval(() => {
    //     graphInstance.clear();
    //     graphInstance.setHorizontalOffset(x);
    //     graphInstance.setVerticalOffset(x);
    //     graphInstance.drawDashLine();
    //     graphInstance.drawAxis();
    //     x += 0.1;
    // }, 1000);
};

export default testAndDemo;
